import { HikeReportComment } from './hike-report-comment';

describe('HikeReportComment', () => {
  it('should create an instance', () => {
    expect(new HikeReportComment()).toBeTruthy();
  });
});

import { HikeReportPipePipe } from './hike-report-pipe.pipe';

describe('HikeReportPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HikeReportPipePipe();
    expect(pipe).toBeTruthy();
  });
});

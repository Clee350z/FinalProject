import { TrailFiltersPipe } from './trail-filters.pipe';

describe('TrailFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new TrailFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
